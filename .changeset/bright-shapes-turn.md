---
"@pptx-glimpse/document": minor
---

`addShape` で preset adjustment、custom geometry、水平・垂直 flip、line の width または height 0 を指定できるようにする。従来の `preset` input は `geometry: { kind: "preset", preset }` に移行する
